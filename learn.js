// Convert object to an array
let users = {
    "01": "rhino",
    "02": "elephant",
    "03": "lion",
}

let userName = Object.values(users)
let userKey = Object.keys(users)
let userEntry = Object.entries(users)